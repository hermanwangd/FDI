# ADR-004 — Standalone Governing Content

A Multica handoff bundle is invalid if an active governing module is represented only by an ID, external pointer, or placeholder README. Every active module must resolve to local content and a local digest. `PROJECT-TREE.txt` must enumerate all Markdown files. FT-T2 must physically contain six contract documents/schemas, five Skill documents, and the helper workflow.
