# Skill — PA-Codebase-Inventory

> **Physical status:** Contract-derived standalone implementation profile; execution-verified NOT_CLAIMED. Governing semantics are in Product Asset Maintenance Skill Contracts v0.1.

Maintains PA-03 `CB-01_REPOSITORY_INVENTORY` via `CREATE|REFRESH|RECONCILE|CORRECT`.

Required behavior: bounded canonical repository enumeration; stable repo identity; aliases/lineage; lifecycle; approved ownership source; qualified product/system membership; bounded technical fingerprint; field-level provenance; active-Asset comparison; create/archive/rename/move/replacement/split/merge detection; explicit identity conflicts; scope-qualified completeness; semantic diff; publication eligibility; MaintenanceBundle.

Default upstream access is read-only. Semantic ambiguity requiring judgment yields Human approval requirement. It never declares current feature impact or mutates Layer 1.
