# Multica Project Instructions

Read `PROJECT-OVERVIEW.md` first, then `governance/CURRENT`, `governance/approved-source-lock.json`, `STATUS.json`, `DEVELOPMENT-BACKLOG.md`, and only then detailed specs/contracts/Skills.

Never infer governing semantics from a module ID alone. Every active module ID MUST resolve to a local file/directory in the approved-source lock and pass digest verification.

Multica owns execution choices (agents/models/parallelism/retries/escalation/cost observability) but must not redefine T1–T4, HERM-211 helper identities, canonical gates, Product Asset publication authority, current-evidence rules, or T4 independence.
