# S04 — PM Intention and Durable Human Resume

Classification: PASS after revision-gated correction.

The scenario first produced `WAITING_FOR_INPUT` with unresolved identity/retry questions and `implementationAuthorized=false`. The single PM comment answered exactly those questions. Resume used the same mission and a new run, applied CAS, rejected stale replay, deduplicated duplicate replay, and produced authorized Intention Spec revision 2.

The first two resume artifact deliveries were correctly rejected because of placeholder/stale state references. Reviewer and Verifier both passed the canonical revision 3 delivery. This demonstrates the correction loop and current-revision gating; it does not erase the earlier defects.
