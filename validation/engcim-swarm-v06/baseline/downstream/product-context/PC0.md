# PC0 — no composed Product Context

PC0 is an experimental condition, not a context artifact. The runtime receives
the same raw SPC-MISSION-V1 inputs but no composed Product Context and no
Product Context resolver/preload call. Raw evidence remains available and all
fallback discovery must be measured.
