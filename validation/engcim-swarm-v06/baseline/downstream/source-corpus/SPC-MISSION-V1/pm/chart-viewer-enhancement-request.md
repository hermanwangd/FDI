# PM request: chart viewer interaction

Open a selected chart from the chart list in one interaction. Preserve the
current 404 response behavior and do not change chart-limit semantics.

This complete case is the authorized S04 input. The separate ambiguous case is
kept in `downstream/s04/case-b-ambiguous.md` and must not be silently replaced
by this wording.
