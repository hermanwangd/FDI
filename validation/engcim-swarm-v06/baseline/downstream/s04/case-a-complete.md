# S04 Case A — complete intent

Open a selected chart from the chart list in one interaction. Preserve HTTP
404 `retryable=false`. Do not change chart-limit semantics.

Expected mapping: Product `SPC Demo`, Capability `Chart Management`, Scenario
`Chart Viewing`; implementation authorization is true and blockers are zero.
