# ENGCIM Bootstrap

This directory contains the deployable Supervisor/Multica bootstrap surface:
the exact Supervisor runtime package, overlay source, state templates, CLI
operation material and example configuration.

Live `.claude/engcim/state/*.json` files remain workspace-specific runtime
outputs. The repository stores their schemas/templates here and materializes
the active overlay only during workspace bootstrap.
