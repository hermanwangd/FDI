# 回滾方案：<版本號／hotfix 名>

- 對應發布：<release 連結> ｜ 負責：Swarm Release Manager
- 回滾決策者：<誰有權喊回滾（事故中＝IC＋Release Manager 共同）>

## 1. 回滾觸發條件（可觀察、有閾值）

| 指標 | 閾值 | 觀察視窗 | 動作 |
|---|---|---|---|
| 錯誤率 | > __%（或 burn rate > __） | __ 分鐘 | 立即回滾 |
| p95 延遲 | > __ms 且持續 | __ 分鐘 | 評估後回滾 |
| business KPI（如轉換率） | 下降 > __% | __ 分鐘 | 評估後回滾 |

> 觸發條件必須是監控可觀察的；「覺得不對勁」不是條件。

## 2. 回滾步驟（逐步、可照做）

1. <停止灰度推進／凍結流量切換>
2. <回滾指令或操作（revert commit / 切回 blue / 關 feature flag）>
3. <DB 處置：見 §3>
4. <驗證回滾成功：指標回到 baseline；Verifier 貼證據>
5. <公告與時間線紀錄（事故中走 incident-response 時間線紀律）>

## 3. 資料相容性（必填，最常踩坑）

- migration 可逆性：可逆 / 不可逆（不可逆的降級路徑：<寫明>）
- 新舊版共存期間的 schema 相容性：<舊版能否讀新版寫的資料>
- 已寫入的新格式資料如何處理：<遷移回／保留／手動修復>

## 4. 灰度階段對應

| 灰度階段 | 該階段的回滾動作 | 預估耗時 |
|---|---|---|
| 5% canary | 切回 0% | <估> |
| 100% 全量 | <完整回滾步驟 §2> | <估> |

## 5. 演練紀錄

- [ ] 回滾方案已試跑／桌面演練（日期：；結果：）——未演練的回滾方案視為不存在
